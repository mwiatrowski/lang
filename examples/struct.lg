int_pair := struct {
    first: int,
    second: int,
}

p : int_pair

p.first := 3
p.second := 5
println(p.first)
println(p.second)

# p := [ 7, 8 ]
# [ f, s ] := p
# println(f)
# println(s)

int_pair_pair := struct {
    first: int_pair,
    second: int_pair
}

pp : int_pair_pair
pp.first.first := 1
pp.second.first := 3
pp.first.second := 2
pp.second.second := 4
println(pp.first.first, pp.first.second, pp.second.first, pp.second.second)
