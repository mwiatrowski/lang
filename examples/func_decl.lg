foo := fn (x: int, y: int, z: str) -> (a: int, b: str) {
    a := x + y
    b := z
}
fooRes := foo(3 + 1, -5, "text")

bar := fn (a: str, b: str) -> (s: str) {
    s := a + b
}
barRes := bar("conca", "tenation")
print(barRes)

nop := fn () -> () {}
nop()

nop2 := fn(x: int) -> () {}
nop2(10)
